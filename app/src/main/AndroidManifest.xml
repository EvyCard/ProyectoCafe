<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto_cafe">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto_cafe">
        <activity android:name=".Registrarse"></activity>
        <activity android:name=".Reporte" />
        <activity android:name=".Pantalla_ensayo" />
        <activity android:name=".Pantalla4_2" />
        <activity android:name=".Envase_etiquetado" />
        <activity android:name=".Recepcion_almacenamiento"/>
        <activity android:name=".Menu_principal" />
        <activity android:name=".Login_cafe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>